export const CANVAS_WIDTH = window.innerWidth
export const CANVAS_HEIGHT = window.innerHeight

export const CELL_SIZE = 10

export const MAX_CELL_X = Math.floor(CANVAS_WIDTH / CELL_SIZE)
export const MAX_CELL_Y = Math.floor(CANVAS_HEIGHT / CELL_SIZE)
